<article class="page">
  <header class="page-hero">
    <p class="page-hero__eyebrow">{{ page.title | upcase }}</p>
    <h1 class="page-hero__title">{{ page.title }}</h1>
  </header>
  <div class="prose">
    {{ page.content }}
  </div>
</article>

{% schema %}
{
  "name": "General Page Content",
  "settings": []
}
{% endschema %}
